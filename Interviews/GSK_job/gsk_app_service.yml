apiVersion: v1
kind: Service
metadata:
  name: gsk-app-service
spec:
  selector:
    app: gsk-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: NodePort # or LoadBalancer if available (i.e. not running minikube)
